% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{expand_inline}
\alias{expand_inline}
\title{Inline-expand an expression into literal R code inside the current RStudio document}
\usage{
expand_inline(expr)
}
\arguments{
\item{expr}{An expression to be evaluated and inlined. This is captured and
evaluated in the calling environment.}
}
\value{
Invisibly returns the evaluated value of \code{expr}. The function is
used for its side effect of modifying the active RStudio document.
}
\description{
\code{expand_inline()} evaluates the expression passed to it and replaces the
corresponding \code{expand_inline(...)} call in the current RStudio document with
a literal R representation of the value (via \code{dput()}). This allows you to
“freeze” dynamic expressions—such as \code{Sys.glob()} calls—directly into your
script or package source file.
}
\details{
The function locates the \code{expand_inline()} call in the source using
\code{getParseData()}, identifies the full call expression, evaluates the
argument in the caller's environment, and then rewrites the source code
in-place using \code{rstudioapi::modifyRange()}.
}
